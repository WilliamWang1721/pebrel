name: Validate Windows system notifications

on:
  push:
    branches:
      - ci/windows-system-notifications-validation

permissions:
  contents: read

jobs:
  validation:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - name: Workflow probe
        run: echo validation-workflow-loaded
